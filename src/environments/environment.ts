export const environment = {
  production: false,
  // Base URL for real API - domain only (without /api)
  // Final URLs: baseUrl + /api/Login, baseUrl + /api/payments, baseUrl + /api/admin/overview
  apiUrl: 'https://doctormate.runasp.net'
};
